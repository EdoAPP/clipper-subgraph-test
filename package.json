{
  "name": "clipper-subgraph-test",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ EdoAPP/clipper-subgraph-test",
    "create-local": "graph create --node http://localhost:8020/ EdoAPP/clipper-subgraph-test",
    "remove-local": "graph remove --node http://localhost:8020/ EdoAPP/clipper-subgraph-test",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 EdoAPP/clipper-subgraph-test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
